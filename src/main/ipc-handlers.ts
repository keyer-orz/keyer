/** 
 * 传统 IPC 处理器
 * 
 * 注意：大部分功能已迁移到新的模块化 IPC 系统 (*-module.ts 文件)
 * 这里只保留一些特殊的事件处理，如导航事件等
 */
export function registerIpcHandlers(): void {
  // 所有 API 调用已迁移到新的模块化系统
  // 如果需要添加特殊的 IPC 事件处理（非 API 调用），可以在这里添加
  
  console.log('Legacy IPC handlers registered (mostly migrated to module system)')
}
