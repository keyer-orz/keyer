{
  "name": "keyerext",
  "version": "1.0.0",
  "description": "Type definitions and SDK for Keyer extensions",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "scripts": {
    "build": "npm run build:esm && npm run build:cjs && npm run build:css",
    "build:esm": "tsc",
    "build:css": "mkdir -p dist/components && cp src/components/*.css dist/components/ 2>/dev/null || true",
    "watch": "tsc --watch"
  },
  "keywords": [
    "keyer",
    "extension",
    "types"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
